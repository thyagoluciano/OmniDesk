## 1. Bindings purego para CoreFoundation e ApplicationServices

- [ ] 1.1 Criar `internal/inputshare/darwin_bindings.go` (build tag `darwin`) que faz `purego.Dlopen` de `CoreFoundation.framework` e `ApplicationServices.framework` e usa `purego.RegisterLibFunc` para expor as funções listadas em `design.md` Decisão 2 (CGEventTap*, CGEventPost, CGEventCreate*, CGEventGet/SetIntegerValueField, CGMainDisplayID, CGDisplayPixelsWide/High, AXIsProcessTrustedWithOptions, CFMachPortCreateRunLoopSource, CFRunLoop*, CFRelease, mais os helpers de CFString/CFDictionary que `AXIsProcessTrustedWithOptions` exige). Verificar rodando `GOOS=darwin GOARCH=arm64 go build ./internal/inputshare/...` a partir de uma máquina Linux (cross-build sem cgo, sem SDK) e confirmando que compila limpo.
- [ ] 1.2 Escrever um teste opt-in (`OMNIDESK_DARWIN_LIVE_TEST=1`, mesmo padrão do `OMNIDESK_X11_LIVE_TEST` em `capture_linux_live_test.go`) que só faz o dlopen das duas frameworks e resolve todos os símbolos registrados, falhando com mensagem clara se algum símbolo não existir. Verificar rodando esse teste numa Mac real.

## 2. Backend de captura (`capture_darwin.go`)

- [ ] 2.1 Implementar `newDarwinBackend()`/`Start(ctx, cb)`: criar o `CGEventTapCreate` com `kCGEventTapOptionDefault`, registrar via `purego.NewCallback` o callback que traduz cada `CGEventRef` recebido em uma chamada aos `Callbacks` (`OnMotion`/`OnButton`/`OnScroll`/`OnKey`), e rodar `CFRunLoopRun` numa goroutine dedicada com `runtime.LockOSThread()`. Verificar com o teste opt-in de 1.2 estendido (ou um novo) que injeta eventos via `CGEventPost` num processo separado e confirma que o callback os recebe.
- [ ] 2.2 Tratar a notificação `kCGEventTapDisabledByTimeout` re-habilitando o tap via `CGEventTapEnable` (design.md Risco 2 — mesma categoria do desalinhamento do RECORD que already mordeu este projeto em `kvm-input-sharing`/`record_conn_linux.go`, tratar como risco real desde o início). Verificar registrando um log quando o timeout ocorrer e confirmando que a captura volta a funcionar sem reiniciar o processo.
- [ ] 2.3 Implementar `ScreenRect()` via `CGMainDisplayID` + `CGDisplayPixelsWide`/`CGDisplayPixelsHigh`. Verificar comparando o valor retornado com a resolução real configurada em Preferências do Sistema → Displays numa Mac real.
- [ ] 2.4 Implementar `Stop()`: parar o `CFRunLoop` (`CFRunLoopStop`), invalidar o tap, esperar a goroutine de captura encerrar — mesmo padrão de teardown limpo que `x11Backend.Stop` já usa (não fechar recursos que uma injeção em andamento ainda possa precisar). Verificar chamando `Start` seguido de `Stop` em loop (10x) e confirmando que não trava nem vaza a goroutine (checar com `runtime.NumGoroutine()` antes/depois).

## 3. Injeção de eventos (`Inject`)

- [ ] 3.1 Implementar `Inject(ev Event)` para `MouseMoveEvent`/`MouseWarpEvent` via `CGEventCreateMouseEvent` + `CGEventPost`, `MouseButtonEvent` (com o mapeamento de botão equivalente ao `x11ButtonDetail` de `capture_linux.go`), `MouseScrollEvent` via `CGEventCreateScrollWheelEvent`, e `KeyEvent` via `CGEventCreateKeyboardEvent` usando o keymap da seção 4. Verificar com o teste ao vivo de round-trip da seção 6.

## 4. Tabela de tradução de keycode (`keymap_darwin.go`)

- [ ] 4.1 Construir a tabela literal `[]struct{ VK byte; HID HIDUsage }` cobrindo o mesmo conjunto de teclas que `keymap_linux.go` cobre hoje (letras, dígitos, pontuação padrão, modificadores, teclas de função, setas, navegação/edição comuns), usando as constantes `kVK_*` documentadas do HIToolbox como referência. Verificar com um teste de unidade que confirma que toda entrada de `keymap_linux.go`'s `evdevToHID` tem uma contraparte aqui (mesmo conjunto de `HIDUsage`, permitindo que o valor do keycode nativo difira).
- [ ] 4.2 Implementar `buildKeycodeToHIDMap`/`buildHIDToKeycodeMap` espelhando as funções homônimas de `keymap_linux.go`, e o log de aviso para keycode sem mapeamento conhecido (mesma mensagem/formato usado em `capture_linux.go`'s `dispatchKey`). Verificar com teste de unidade que uma tecla sem mapeamento é descartada (não chama `OnKey`) e gera o log esperado.

## 5. Suppress/Release

- [ ] 5.1 Implementar `Suppress()`/`Release()` como um `atomic.Bool` verificado pelo callback do tap (design.md Decisão 3): quando suprimido, o callback devolve `NULL` em vez do evento original, impedindo a entrega a qualquer outro processo. Verificar com um teste ao vivo: iniciar uma sessão simulada (chamar `Suppress`), injetar uma tecla via um processo externo (`osascript` ou `cliclick`) e confirmar que ela não chega a nenhum app local; chamar `Release` e confirmar que a mesma tecla volta a ser entregue normalmente.
- [ ] 5.2 Confirmar que o `Suppress` continua permitindo que o próprio RECORD/tap capture os eventos pra encaminhamento mesmo enquanto suprime a entrega local (mesma distinção que o X11 já faz entre "captura" e "entrega") — verificar com o mesmo teste de 5.1, checando que o evento suprimido ainda aparece no `Callbacks.OnKey` do lado que está enviando.

## 6. Onboarding de permissões

- [ ] 6.1 Implementar a checagem/prompt de Acessibilidade via `AXIsProcessTrustedWithOptions` (com a opção de prompt ativada) chamada em `Backend.Start`, retornando um erro descritivo se não concedida — seguindo o padrão já usado por `capture_linux.go` de degradar com log claro em vez de travar (`ErrUnsupportedPlatform`-style, mas com mensagem específica de qual permissão falta). Verificar numa Mac real sem a permissão concedida: confirmar que o prompt do sistema aparece e que o erro retornado nomeia "Acessibilidade".
- [ ] 6.2 Tratar o caso em que `CGEventTapCreate` falha mesmo com Accessibility concedida (sinal prático de que falta Monitoramento de Entrada, já que não há API pública separada pra checar isso — design.md Decisão 5): mensagem de erro cobrindo as duas permissões e como habilitá-las (Configurações do Sistema → Privacidade e Segurança → Acessibilidade / Monitoramento de Entrada). Verificar numa Mac real concedendo só Acessibilidade e confirmando que a mensagem também menciona Monitoramento de Entrada.
- [ ] 6.3 Expor a mensagem de erro de 6.1/6.2 na dashboard/bandeja (mesmo local onde hoje aparece "input sharing not available" quando `InputMgr` é nil) em vez de só no log — verificar abrindo o dashboard numa Mac sem as permissões e confirmando que a mensagem aparece na UI, não só no terminal.

## 7. Registro no hook de plataforma e validação end-to-end

- [ ] 7.1 Registrar `newDarwinBackend` em `newPlatformBackend` (`capture.go`), atrás de build tag `darwin`, seguindo exatamente o padrão de `capture_linux.go`'s `func init()`. Verificar com `GOOS=darwin GOARCH=arm64 go build ./...` limpo a partir do Linux.
- [ ] 7.2 Rodar o roteiro completo da seção 9 de `kvm-input-sharing/tasks.md` (pareamento, permissão de controle, arranjo de telas, cruzar a borda, os três escapes, queda de rede, sensação de latência) entre um nó macOS real e um nó Linux/X11 real — a validação de duas máquinas físicas que `kvm-input-sharing` 9.1 ainda não tinha fechado. Documentar o resultado (incluindo qualquer bug novo encontrado, no padrão detalhado que `kvm-input-sharing/tasks.md` seções 7.6–7.10 já usam) diretamente em `kvm-input-sharing/tasks.md`, já que é essa mesma tarefa 9.1 que está sendo fechada.
- [ ] 7.3 Confirmar que `panicHotkeyCombo` (`manager.go`, hoje `Ctrl+Alt+Shift+Esc`) é fisicamente alcançável e funciona igual num teclado Mac (teclas modificadoras diferentes fisicamente, mas os HID Usage Codes usados no combo — Control/Alt/Shift/Escape esquerdos — são os mesmos canônicos; confirmar que o keymap da seção 4 mapeia as teclas físicas correspondentes do Mac pra esses mesmos códigos). Verificar testando o atalho de pânico numa sessão ativa com o Mac como remetente.
